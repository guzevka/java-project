package ru.rentalofpremises.repositories;
/*
import org.springframework.data.jpa.repository.JpaRepository;
import ru.rentalofpremises.models.Apartment;
import ru.rentalofpremises.models.Rent;

import java.time.LocalDate;
import java.util.List;

public interface RentRepository extends JpaRepository<Rent, Long> {
    List<Rent> findByDate(Long id, LocalDate start, LocalDate end);
    Integer getAmount(Long id);
}*/
